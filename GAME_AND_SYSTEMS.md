# PACKSPIRE ゲーム内容・システムまとめ

最終更新: 2026-08-04
対象: `unity/PackspireUnity` の現行実装

この文書は、現在のゲーム構造と実装済みの基盤をまとめる正本です。2026年8月に採用した荷物・航路・役職試作の変更は [SYSTEM_REWORK_2026-08.md](SYSTEM_REWORK_2026-08.md) を優先します。キャラクターは既存の配達人から選び、キャラクリ機能は対象外です。将来案や未実装アイデアは [UNIMPLEMENTED_IDEAS.md](UNIMPLEMENTED_IDEAS.md)、作業順は [ROADMAP_AND_TASKS.md](ROADMAP_AND_TASKS.md)、数値とIDは [DATA_REFERENCE.md](DATA_REFERENCE.md)、役職・装備・配達印を追加する際の方針は [CONTENT_DESIGN_GUIDELINES.md](CONTENT_DESIGN_GUIDELINES.md) を参照してください。

## 1. ゲーム概要

PACKSPIREは、装備と荷物を収納術式へ詰める配置パズル、装備から生まれる戦闘デッキ、分岐する配達航路を一つの流れにしたローグライトです。

核となる考え方は次の通りです。

- バッグへ入れた装備が、そのランで使える能力を決める。
- 収納術式の隣接LINKが戦闘札を強化し、色一致が配達印を生成する。
- 配達印は区間日数、地点で生じる追加日数、印の再装填へ介入し、戦闘札とは役割を混ぜない。
- 航路地点はイベント、戦闘、回収、中継所、最終配達へ役割を分ける。
- 時間と危険を引き受けるほど回収機会が増え、安全な経路との比較が生まれる。

## 2. 基本ゲームループ

1. 拠点でキャラクター、役職、勢力、収納術式、持ち込む装備を整える。
2. 荷造り画面で複数マス形状の装備を回転・配置する。
3. 遠征先を選び、分岐する配達経路台帳を開く。
4. 装備の色一致と役職から得た配達印で、区間日数や地点遅延へ介入する。
5. 経路上の区間を選び、地点種別に応じてイベント、戦闘、回収、中継、配達を解決する。
6. 地点解決後に台帳へ戻り、10区間目の配達先で遠征を完了する。
7. 将来の地点ミニゲームは共通の地点結果を返す追加コンテンツとし、標準地点へ強制しない。

## 3. 収納術式と荷造り

荷物も装備と同じ収納術式へ入れる。回収直後の荷物は未整理荷物として保留し、中継所を選んだ時だけ収納術式へ移して配置を組み直せる。中継所で確定した配置からLINKと色を再評価するが、使用済みの印は復活しない。LINKは戦闘札、色は航路用の配達印、形状は荷室、耐久は共通コストを担当する。詳細は [SYSTEM_REWORK_2026-08.md](SYSTEM_REWORK_2026-08.md) を参照。

### 装備配置

- 標準の論理盤面は6×4。
- 装備は1マスからL字、縦長、横長など複数マスの形を持つ。
- 配置時は全占有マスが当たり判定を持つ。
- 回転後の形状と占有マスを使って衝突・範囲外判定を行う。
- 装備の色属性は火・水・風・土の4種。

### 収納術式の4構成

| 構成 | 役割 |
|---|---|
| 術核 Core | 盤面サイズ、属性配列、回転制限 |
| 導線 Conduit | 属性一致による数値補正 |
| 共鳴 Resonance | 隣接LINK、カード置換・上位化 |
| 安定性 Stability | 耐久消費と過負荷 |

現行の標準構成は `standard / classic / classic / stable` です。収納術式の部品は差し替え可能なため、将来のバッグは単純なマス数増加だけでなく、回転制限、属性盤面、共鳴規則、耐久リスクで差別化できます。

### カード生成

- 装備は戦闘用カードを1枚以上生成する。
- 剣、盾、短剣など一部装備は同じ基本カードを2枚生成する。
- 隣接関係、色一致、役職、勢力、継承装備などの補正を反映してラン用デッキを構築する。
- 色一致数から航路で使う配達印と使用回数を生成する。
- 現在の方針では「この装備の戦闘カードだけ入れない」といった個別のデッキ採否は設けない。

## 4. 格子盤ダンジョン

GridBoardは旧探索方式および将来の特殊地点ミニゲーム候補として残す。標準の配達航路では地点ごとに起動せず、遠征全体は9段階の二択分岐と配達先からなる10区間を、地点種別ごとの解決処理で進める。

### 盤面モデル

- `GridBoard` が現行の探索方式。
- 通常の配達経路からは起動しない。開発用・特殊遠征用として残す。
- 局地盤は地点データに応じて7～9マス級を使う。
- セルは床、通行不可、虚無、開始、目的地を持てる。
- 長方形の全面床を前提とせず、欠け、穴、通路を持つ不定形盤面に対応している。
- 敵は地形やセル配置へ焼き込まず、座標・行動型・警戒状態を持つ独立した盤面エンティティとして扱う。
- 敵の行動型は追跡、巡回、待伏せ。探索ターン終了時に移動し、プレイヤーと同じセルへ入ると戦闘へ移行する。
- 行動型、索敵距離、1ターンの移動歩数、巡回半径は敵のScriptableObjectデータで個別に設定する。
- 地形の輪郭と通行可否は最初から把握できる。現在地の周囲1マスを基本視界とし、ルート移動で通過した各地点の周囲も解析済みとして記録する。
- 固定物は視界外でも「何かがある」ことだけ分かり、視界へ入るか探索カードで公開すると灯、イベント、出口などの正体が判明する。
- 敵は初期反応または最後に目撃した位置だけを盤面へ残す。視界外で移動した敵をリアルタイム追跡せず、再び視界へ入った時点で位置と詳細を更新する。
- キャラクターのマスターデータから視界補正を与えられる。時間経過で探索済み記憶を失う仕組みの入口もあるが、現在は無効。

### 進行フェーズ

| フェーズ | 内容 |
|---|---|
| Place | 探索術式カードを盤面へ配置 |
| Path | 曲がり上限の範囲で移動ルートを指定 |
| Run | 指定ルートを実行し、接触物を順に解決 |
| Done | 区画終了または帰還処理 |

移動は、盤面上の経路を左クリックで選び、実行中はキャラクターへカメラを寄せて追従する構成です。

### 操作

- 左クリック: マス選択、移動先・ルート選択。
- 右ドラッグ: 盤面をつかんでパン。
- ホイール: ズーム。
- ルート作成中: 一手戻る、開始、中止の補助操作を表示。
- マスホバー: 右下へマス詳細を表示。上下枠を使わず左アクセント線と暗い背景で盤面を隠しすぎない。
- カード選択中: 右側へ大型プレビューと配置可能セルを表示し、盤面へ重ねる。

### 完成版サンプル: 紫晶の封鐘塔

既定ダンジョン `old_spire` は、今後35以上のダンジョンを作るための完成版縦切りサンプルです。共通ランダム生成だけでなく、ダンジョンSOが区画ごとのサイズ、敵プール、イベントプール、配置数、灯り数、目的文、最終ボスを指定します。各区画は個別の不定形レイアウトを持ち、欠けや内部の奈落を含みます。外周の盤面外セルは最初から描画せず、正方形の盤面外枠として見せません。

1. 欠けた入口 — 待伏せ・巡回敵と、灯／楔の基本。
2. 鐘鎖の交差路 — 追跡敵を含む2体配置と複数イベント。
3. 反響保管層 — 毒・防具破壊・脆弱、修復か代償かの判断。
4. 封鐘心臓部 — 帰還点を置かず、Tier 3の荷喰らい撃破で踏破。

戦闘報酬は専用プール `old_spire_trials` を使います。武器、防具、ルーン、道具、呪物を含み、候補3つのまとまりが戦闘ごとに巡るため、複数周で主要な装備方向を比較できます。最終的な背景・セル・イベントアートとUI微調整は別工程ですが、探索→イベント→戦闘→報酬→進出／帰還→ボス踏破の進行基盤はこのダンジョンを基準とします。

通常構成では最初の敵を進出点付近の番兵として置き、確定した移動ルートの近くにいる敵へ物音を伝えて警戒させます。地形が見えても敵の現在位置を完全追跡できないため、単純な全回避は安定しません。索敵、霧、消音などへ特化した構成には、戦闘を避ける余地を残します。

### 探索オブジェクト

現行データは次の配置種別を扱います。

- 灯
- 霧
- 封印
- 敵
- イベント
- 次区画
- 帰還
- 破壊不能な時間圧力施設

灯・霧・封印などの配置術式はルート完了ごとに成長し、成長値3で成熟します。

- 成熟した灯: 周囲2マスを継続して照らす。
- 成熟した霧: 敵駒の侵入を防ぎ、追跡経路を迂回させる。
- 成熟した封印: プレイヤーの導線と敵駒の双方を遮断する。

### 時間圧力

現行実装ではDoom値を内部圧力として使用します。

- Doom 2ごとに1段階、最大3段階。
- 段階ごとに敵HPを約12%増加。
- 段階ごとに敵攻撃値を加算。
- 段階ごとに敵の索敵距離も拡大。

各区画にはダンジョンごとの破壊不能施設を通行不能地形上へ生成します。施設を視界へ入れてホバーすると現在の圧力を確認できます。恒久HUDへ固定カウンターは置きません。

配達経路では各区間の所要日数と地点の解決結果が配達期限へ接続する。追跡と積荷耐久を別の航路メーターにはしない。将来のミニゲームも追加日数と成果を同じ結果契約で返せるが、現時点の標準地点にはミニゲームを必須化しない。

地点報酬も経路へ戻る。回収地点は未整理荷物、中継所はHP・使用済み配達印1回分の回復と収納の組み直しを与える。戦闘地点は戦闘、事象地点はイベント選択を使う。

## 5. 戦闘

### 同一画面への移行

敵に接触すると、別の戦闘背景へ遷移せずGridBoard上で戦闘状態になります。

- 中央: 大型の敵イラスト。
- 敵の下: 敵名、HP、シールド。
- 左: 次の行動と予測値。
- 右: 選択カードの詳細、ダイス表示。
- 下: 手札。通常時は下へ沈み、ホバー・選択時に持ち上がる。
- 左端: プレイヤーの縦型HP・シールドゲージ、顔が見える立ち絵、エネルギー。
- 右下: アクティブスキルとターン終了。
- 右上: 戦闘ラウンド。

探索時の上部大型バナーは廃止し、盤面を最大限使う現在のレイアウトを基準とします。

### 基本ルール

- 基本エネルギー: 3。
- 初期手札: 5枚。
- ブロックは攻撃を軽減する。
- 攻撃強化、弱体、脆弱、装甲破壊などの状態を扱える。
- 敵は次の行動を予告する。
- 配達航路の追跡戦では戦闘終了後に航路台帳へ戻る。GridBoardから開始した特殊戦闘だけは元の盤面へ戻る。

### ダイスダメージ

固定ダメージ表記ではなく、攻撃は原則として `2D6 + 補正` で見せます。内部の期待値から2D6の中心値7を引いて補正値を求め、実際のロール結果を適用します。

例:

- 期待値5 → `2D6 - 2`
- 期待値7 → `2D6 + 0`
- 期待値11 → `2D6 + 4`

ダイス結果は右のカード・ダイス領域へ表示し、敵イラストや手札へかぶせません。

## 6. イベントと報酬

- イベントは盤面常設の詳細カラムではなく、接触時のポップアップまたは専用イベント画面で解決する。
- イベント終了後はGridBoardへ復帰し、移動状態を解除して操作を再開する。
- 次区画と帰還も確認ポップアップを経由して進行する。
- 戦闘勝利後は報酬画面へ接続できる。

## 7. 拠点とメタ進行

現在の画面基盤には以下が存在します。

- キャラクター選択
- 拠点
- ステータス・役職
- 保管庫
- 家宝・継承装備
- 勢力
- 遠征選択
- 荷造り
- 報酬
- ショップ
- 図鑑
- ゲームオーバー / ゲームクリア

保存データは、所持金、保管庫、ロードアウト、役職レベル、勢力評判、発見済み装備・敵、消耗品、選択中キャラクターなどを保持します。ラン中データはHP、所持品、配置、デッキ、区画状態、戦闘状態を保持します。

## 8. UI実装の構造

### 画面ルート

`PackspireUiFoundation` が画面を構築し、`ScreenId` で切り替えます。主な画面は `Hub`、`Pack`、`GridBoard`、`Reward`、`Shop`、`Event` です。

### スタイル読込順

後から読み込まれるUSSほど上書き優先度が高くなります。

1. `PackspireTheme.uss`
2. `PackspirePacking.uss`
3. `PackspireRoute.uss`
4. `PackspireGridBoard.uss`
5. `PackspireRoster.uss`
6. `PackspireBattle.uss`
7. `PackspirePolish.uss`
8. `PackspirePopDark.uss`
9. `PackspireManagement.uss`
10. `PackspireMeta.uss`
11. `PackspireCommerce.uss`
12. `PackspireHub.uss`

GridBoardと荷造りは役割別のpartial classへ分割されています。新規UIは一つの巨大USSや一つの巨大C#へ戻さず、画面・責務ごとに追加します。

### 現行の重要な制約

- UI Toolkitで再帰的なレイアウト変更を起こす処理を避ける。
- Unityが対応しない再帰疑似クラスや複雑すぎるセレクタをUSSへ入れない。
- 画像フレームは無理に引き伸ばさず、想定比率に合わせた専用素材か9-sliceを使う。
- 盤面、カード、敵、ポップアップのレイヤー順を明示し、ホバー表示が入力を奪わないようにする。

## 9. 実装上の正本

| 内容 | 主なファイル |
|---|---|
| 基本モデル・保存形式 | `Assets/Scripts/Core/Models.cs` |
| ゲームマスター入口 | `Assets/Resources/Packspire/PackspireContentDatabase.asset` |
| カテゴリ別マスター | `Assets/Resources/Packspire/Content/*.asset` |
| マスターデータ型・検証 | `Assets/Scripts/Core/PackspireContentDatabase.cs` |
| Resources読込境界・キャッシュ | `Assets/Scripts/Core/PackspireResources.cs` |
| 入力方式の境界 | `Assets/Scripts/Core/PackspireInput.cs` |
| 装備・カード等の互換API | `Assets/Scripts/Core/GameCatalog.cs` |
| キャラクター互換API | `Assets/Scripts/Core/CharacterCatalog.cs` |
| 拠点施設互換API | `Assets/Scripts/Core/HubFacilityCatalog.cs` |
| 収納術式互換API | `Assets/Scripts/Systems/StorageFormulaCatalog.cs` |
| 荷造り計算 | `Assets/Scripts/Systems/BackpackSystem.cs` |
| デッキ構築 | `Assets/Scripts/Systems/LoadoutSystem.cs` |
| 格子盤進行 | `Assets/Scripts/Systems/GridBoardSystem.cs` |
| 戦闘計算 | `Assets/Scripts/Systems/BattleSystem.cs` |
| 保存 | `Assets/Scripts/Systems/SaveSystem.cs` |
| GridBoard UI | `Assets/Scripts/UI/PackspireUiFoundation.GridBoard*.cs` |
| 荷造りUI | `Assets/Scripts/UI/PackspireUiFoundation.PreparationScreens*.cs` |

装備、カード、役職、敵、ダンジョン、バッグ、勢力、キャラクター、施設、イベント、商人、報酬、状態異常、収納術式は、入口SOから参照する4つのカテゴリ別ScriptableObjectを正本とします。セーブとラン状態だけはJSONで保存し、マスターデータを安定した文字列IDで参照します。

セーブは現行・書き込み途中・バックアップの3段階で保持し、旧版JSONは段階的なマイグレーションを通します。ビルド開始前にはマスターデータ検証が自動で走り、主要なデータ参照・セーブ移行・戦闘計算・格子盤初期化はEditModeテストで確認します。

ランタイムの `Resources.Load` は `PackspireResources` だけを入口とし、同一アセットの反復検索をキャッシュします。将来Addressablesへ移行する場合も、この境界の実装を差し替えます。入力は現状のプロジェクト設定に合わせて旧Input Managerへ統一し、ゲーム側からは `PackspireInput` を通します。新Input Systemを導入するまでは両方式を混在させません。

マスターデータ内の日本語は正規化済みです。UIコードなどに残る古い直書き文字列は、各画面の正式データ投入時に引き続き整理します。
